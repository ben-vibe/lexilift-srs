# ─── Local scripts only (never commit real values) ───
# Used by: npm run words:build (optional), node scripts/batch_generate_oxford.mjs
OPENAI_API_KEY=

# ─── Optional: Supabase (app works fully without these) ───
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# ─── Vercel: set secrets in Dashboard → Settings → Environment Variables ───
# OPENAI_API_KEY     → Server only (if you add /api/translate later)
# Do NOT prefix OpenAI keys with VITE_ (that exposes them in the browser).
